import React, { Component } from 'react';
import { connect } from 'react-redux';
import { Container } from 'native-base';

function mapStateToProps(state) {
    return {

    };
}

class ScreenReserve extends Component {
    render() {
        return (
            <Container>
                
            </Container>
        );
    }
}

export default connect(
    mapStateToProps,
)(ScreenReserve);